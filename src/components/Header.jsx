import "../Styles/header.css";
const Header = () => {
    return (
        <header>
            <h1>Pokemon Memory Game</h1>
            <p>Click on a Pokemon to earn points, but don't click on any more than once!</p>
        </header>
    );
}

export default Header;